# 星棋派对 · 云朵浮岛可玩版验收

final result: passed

范围：已选第一种设计的可玩实现；不表示与概念图逐像素相同，也不表示已达到商业产品全部功能或通过真实手机性能测试。

## 对比证据

- 原始视觉：`design/approved-cloud-party.png`，1586×992 设计板，包含桌面、手机、选棋和获胜状态。
- 桌面：`qa/desktop-final.png`，1440×900 CSS，2880×1800 截图，2×密度。
- 手机：`qa/phone-final.png`、`qa/mobile-win.png`，390×844 CSS，780×1688 截图，2×密度。
- 平板：`qa/tablet-win.png`、`qa/tablet-overhead.png`，820×1180 CSS，1640×2360 截图，2×密度。
- 截图由真实 Chrome DevTools 的设备尺寸及截屏功能取得，操作通过 CUA 完成。截图不包含浏览器外框；本地 QA 文件不进入网站发布包。
- 原稿与桌面、手机、手机获胜图在同一比较输入中打开，另由独立审查代理复核。按原稿的对应区域比较构图与状态；原稿不是这些精确 CSS 尺寸，未做像素误差分数。字体、棋子轮廓、合法落点、底部按钮和胜利面板在原始高分辨率图中均可读，无须另外裁片来判断。
- 原稿中的非法棋局和错误坐标不作为规则目标；实现截图使用标准开局中选中 g1 马的真实合法状态。原稿的一次按钮被补全为再玩、下一挑战和查看棋局的真实操作。以上是明确的产品差异。

## 修正记录

1. P2：初始桌面/手机棋盘过小，白棋明暗过弱。调整相机角度、棋盘区域、白棋材质及灯光；最终桌面和手机截图棋盘完整，白棋可辨，操作区不遮挡棋格。
2. P2：只按棋盘地面命中会把棋子头部错认成后排。加入真实模型射线命中；在手机比例下直接点击 d6 后的头部，AX 确认选中 d6，而不是 d7；随后正常 Qd8 将死。
3. P2：竖屏平板用了过度裁切的横向背景，棋盘偏小。改用竖向背景并增大棋盘；将伙伴移至岛面边缘，平板双车残局真实走通。
4. P2：平板切到俯视时伙伴遮住 a8/h8。竖屏俯视时收起两侧伙伴，保留建议按钮和回合头像；`qa/tablet-overhead.png` 证明所有棋格完整显示，立体视角仍保留两位伙伴。
5. 主流程审查修复：加载中操作守卫、无效导入不改原棋局、AI 故障后仍可打开设置、声音按钮图标和状态一致。

## 五项视觉检查

- 字体：标题使用 PingFang SC / 系统黑体粗字，正文使用系统圆体回退。没有复制原稿的手写形状；中文字完整，主要操作没有截断。原稿更具手绘感，列为 P3。
- 布局：云岛为主场景，棋盘居中，桌面伙伴分居两侧，手机伙伴在棋盘上方，工具置底；手机棋盘接近全宽。平板俯视专注于棋盘。无持久控件溢出或棋格遮挡。
- 色彩：奶油与浅蓝棋格、深蓝/奶油棋子、薄荷/桃粉伙伴、浅蓝圆按钮、青绿合法提示与粉色建议保留；白棋与浅色格有可辨轮廓。
- 资产：独立生成的天空、岛屿、角色、木牌与王冠；真实 GLB 棋子和棋盘，非整屏设计图。素材边缘和透明度可用，没有占位图。程序模型的棱角、平面棋盘及硬投影仍比概念图更明显，列为 P3 后续精修，不称 1:1。
- 文案：提供具体可操作的选棋、将军、建议、悔棋、升变与结果说明；不承诺感情结果，不以获胜换取亲密。伙伴建议须由玩家采纳。

## 实际操作与技术验证

- 浏览器：标准开局 e2-e4，电脑 c7-c5；伙伴 g1-f3 建议先保留，采纳后电脑回应；悔棋撤回双方一轮；刷新恢复 e4/c5。
- 手机：f7-f8 弹出四种升变，选择后并真正将死；后车合力 Qd8 将死。
- 平板：Rb7，电脑 Kg8-f8，Ra8 将死；获胜后回看及切换俯视。
- 离线：正式包在 `/star-chess-party/` 子路径注册并重载；停止本地 HTTP 服务且 curl 确认不可连接后，浏览器刷新仍恢复棋局，e2-e4 后电脑 e7-e5 正常回应。
- 18 项自动测试通过，覆盖王车易位、受攻击路线、吃过路兵、四种升变、将死/逼和/重复和棋、完整历史恢复、无效存档、悔棋、所有短残局、Worker 取消/超时/迟到回复。正式构建通过。
- 主流程额外隔离检查覆盖冷启动、非法 settings 导入、正常导入同步、声音状态与 AI 失败后设置入口。Service Worker 的版本隔离、整版校验、安装失败及缓存回填失败另经独立审查。
- Chrome 控制台未见应用运行错误；所见警告来自浏览器 MetaMask 扩展，已消除本应用旧阴影类型的警告。构建有主 JS 体积提示（约 178 KB gzip），不等于性能实测。

## 剩余边界

- 真实 iPhone/iPad 的触摸、音频、内存与帧率未测；Chrome 设备尺寸预览不能代替实机。
- 团团是否愿意玩第二局、当前 AI 难度是否合适仍需真实试玩；不会以本次验收推断这些结果。
- 这是一版完整规则和三个合作短挑战的游戏，不含账号、跨设备同步、远程联机或订阅。

## 发布检查

- [x] 独立运行资源、源码、第三方许可及 Stockfish 对应源码/NNUE随包提供。
- [x] 素材记录移除本机绝对路径；发布目录去掉多余原 PNG 与本机 tar 元数据。
- [x] 已选方向及关键交互通过本轮验收，无剩余 P0/P1/P2。
- [x] GitHub Pages 发布成功：代码提交 `808a362`，工作流 `35179561908`。正式网址的 37 项运行资源哈希全部通过，源码与许可页 200；真实浏览器完成 e2-e4 / c7-c5，并刷新恢复。逐项结果保存在本地 `qa/release-evidence.json`。
