# 星棋派对 · 已批准画面再对齐

final result: passed

本轮范围：只修视觉呈现，保留玩法、状态、存档、电脑和声音行为。2026-09-17 用户指出上一版与已批准图差距过大；本报告取代上一轮宽松的视觉判定。旧报告将棋面、棋子和透视错位列为 P3 不准确，本轮按 P1/P2 修正。

## 对照证据

- 视觉真值：`design/approved-cloud-party.png`，1586×992；桌面内容裁框 `[23,38,1112,639]`，手机内容裁框 `[1158,38,408,927]`。
- 最终桌面：`qa/desktop-final-v9.png`，CSS 1112×639，2×密度；并排对照 `qa/desktop-comparison-v9.png`。
- 最终手机：`qa/phone-final-v9.png`，CSS 408×927，2×密度；并排对照 `qa/phone-comparison-v9.png`；细节 `qa/pieces-comparison-v9.png`。
- 平板：`qa/tablet-final-v11.png`，CSS 768×1024；短屏手机：`qa/short-phone-final-v11.png`，CSS 375×667。v11 仅调整兔子图层，棋子/主布局与 v9 相同。
- 真实将死后的结算：`qa/desktop-result-v6.png`、`qa/phone-result-v6.png`；随后未改结算 UI。
- 第一轮错误与迭代保留在 `qa/*comparison-v2/v3/v4/v5/v6/v7/v8.png` 等本地文件。QA 截图不参与游戏运行，也不将完整设计稿嵌入页面。
- 对照图将浏览器截图按 2:1 下采样到 CSS 像素，再与原稿对应内容裁框并排，避免不同像素密度产生伪差异。
- 原稿棋局不是合法标准局面；本轮使用标准开局、真实走子后的选中状态和既有短局，对照相同视觉部位，不改规则来模拟插画棋局。

## 五个阻断验收面

| 验收面 | 结果与依据 |
| --- | --- |
| 整体视觉相似度 | 通过本轮对照：浮岛、棋盘占比、角色与主操作构图接近已选方向，实时 3D 细部仍有差别。 |
| 主要锚点 | 棋格四角按参考校准；消除旧版石台和棋盘的两套透视及双重厚框；手机城堡和探出角色位置重新匹配。 |
| 非代码资产 | 独立宽/高环境及透明城堡均已开图，记录见 `design/environment-v2.json`；角色、木牌、皇冠沿用已批准资产；未用整页截图替代实现。 |
| 文字与图形 | 本地 ZCOOL KuaiLe 字体、标题宽度、头像裁切、回合 pill 和椭圆工具按钮对齐；Phosphor 图标加粗，合法落点直径增至参考量级。 |
| 响应式和状态 | 桌面/普通手机/短屏/平板/选中/真实将死结算截图已检查。v9 发现兔子遮住短屏 g8/h8、平板 h8，v11 改层级后完整可见。 |

## 修正与复验记录

1. P1 棋盘透视、双重厚框：准备独立环境 v2，模型改为薄嵌边，相机按四角求投影，棋格重新贴合石台。
2. P1 俯视只剩头顶、马头像挤出平板：马、象、车改为圆曲面；物理相机最大仰角 48°，通过纵向投影校准保持棋盘四角不动，恢复正面体积。
3. P1/P2 材质与光影：暖白/深蓝棋子，浅弧圆边棋格，局部环境反射、宽高光和可见柔阴影；保留按需渲染。v5 过白返修，最终 v9 暖白保留暗部。
4. P2 手机角色/按钮/字体/头像比例：按实际可见区域重新布置，宽度与原稿逐项比较；合法落点圆点从 .105 增至 .155 格半径，选中圈增加柔光。
5. P1 装饰遮挡：v9 的兔子覆盖棋子，v11 将装饰兔子放到棋盘之后；实际截图确认 g8/h8 头部完整。企鹅未见遮挡，保持 v9。
6. 独立只读复核结论：已审查截图中无剩余 P0/P1/P2。P3 为实时模型高光和细部造型与设计渲染稿的差异；不声称逐像素复刻。

## 逻辑与技术边界

- `src/main.mjs`、`src/game.mjs`、`src/computer.mjs`、`src/storage.mjs`、`src/sound.mjs` 与上一版完全一致。
- 18 项现有自动测试通过；最终生产构建通过，运行资源约 5.43 MiB（42 个文件）。Three.js 单包体积仍有构建提示，未通过修改玩法/加载逻辑扩大本轮范围。
- 最终 48° 投影在 13 种视口 × 2 视角 × 64 格检查，射线回算最大误差 2.42e-13。
- 加载六类实际 GLB，双方全格位置及跳跃动画包络全部可见；1.9 宽高比上限下，最紧 844×390 仍有 7.8px 顶部余量。
- 真实浏览器通过 Ng1-f3、电脑回应、选择 f3；刷新恢复棋局；后车合力实际 Qd6-d8# 打开胜利弹窗。
- Service Worker 行为模板不变，仅增加字体和新视觉资产的打包清单；源码与字体许可随发布提供。
- 实际 iPhone/iPad 的触摸、GPU 帧率、内存尚未测；截图来自真实 Chrome 页面和设备尺寸工具，不冒充真机验收。
- 发布目标保持项目自己的 GitHub Pages：`https://haowangggggg-lang.github.io/star-chess-party/`。

## 正式发布回读

- 运行版本：`ef8432e7434146fc93c07c453b3cc91489eacac3`；GitHub Actions [35186671942](https://github.com/haowangggggg-lang/star-chess-party/actions/runs/35186671942) 测试、构建、部署全部成功。
- 正式站点 HTML 引用新的宽/高环境、独立城堡、`index-BwKIhYau.js` 和 `index-Br_B_beX.css`。
- 9 个关键线上资源（背景、城堡、棋盘/马 GLB、字体、JS、CSS、SW）均与本地生产构建 SHA-256 相同，记录 `qa/release-verification.json`。
- 原生 Chrome 实际打开正式站点；旧离线缓存首次仍显示旧版，离开游戏页再重新进入后，新画面生效且原有棋局保留。
- 正式站点点选 g1 马，正确出现 f3/h3/e2 等当局合法落点；桌面和手机截图为 `qa/public-desktop-ef8432e.png`、`qa/public-phone-ef8432e.png`。
- 已打开的旧游戏页需全部关闭后重新进入，保持现有 Service Worker 不强行中断当前棋局的行为。
